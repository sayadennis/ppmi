# The purpose of this script is to unpack all the tar files that contain WES VCF files from PPMI

tar_dir = ""
output_dir = ""

# for tar_file in listdir(tar_dir); do 
# tar -zxf $tar_file -C $output_dir

# modify code so that the files are unpacked without creating a folder for each tar file (if possible)
